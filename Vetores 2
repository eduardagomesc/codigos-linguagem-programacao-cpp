#include <iostream>
using namespace std;

int main() {

    int A[3] = {8,6,4};

    int B[3] = {3,2,1};

    int D[3];


    for(int i = 0; i < 3; i++) {


        D[i] = A[i] - B[i];

    }

    cout << "Vetor D = (";

    for(int i = 0; i < 3; i++) {

        cout << D[i];

        if(i < 2)

            cout << ", ";

        else

            cout << ")";

    }

    return 0;
}
